{
  "name": "agent-space",
  "description": "MCP-powered filesystem & git spaces on Cloudflare Workers + Durable Objects",
  "type": "worker",
  "secrets": {
    "API_KEY": {
      "description": "API key used for authentication (X-API-Key header and Git Basic auth password)"
    }
  }
}
